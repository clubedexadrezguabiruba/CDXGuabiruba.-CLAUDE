/**
 * Types gerados automaticamente pelo Supabase CLI.
 *
 * Para regenerar após alterar o schema:
 *   npx supabase gen types typescript --project-id SEU_PROJECT_REF > src/types/supabase.ts
 *
 * Ou se linkado:
 *   npx supabase gen types typescript --linked > src/types/supabase.ts
 *
 * Importe com:
 *   import type { Database } from '@/types/supabase'
 */

// Placeholder — será substituído pelo output do supabase gen types
export type Database = Record<string, never>
